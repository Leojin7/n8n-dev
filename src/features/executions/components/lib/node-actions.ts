export interface NodeActions {
  configure?: {
    label: string;
    icon: string;
    description: string;
  };
  test?: {
    label: string;
    icon: string;
    description: string;
  };
  help?: {
    label: string;
    icon: string;
    description: string;
  };
}
